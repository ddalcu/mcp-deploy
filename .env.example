# Domain (wildcard DNS must point to this VPS: *.deploy.example.com → VPS_IP)
DOMAIN=deploy.example.com

# ACME / Let's Encrypt email
ACME_EMAIL=you@example.com

# MCP Server API key (generated during install)
API_KEY=

# MCP Server Docker image (override for custom builds)
# MCP_IMAGE=ghcr.io/ddalcu/mcp-deploy:latest
